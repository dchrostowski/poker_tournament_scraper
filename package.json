{
  "name": "ls",
  "version": "1.0.0",
  "description": "poker scraper",
  "main": "main.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "mongoose": "^6.4.6",
    "ws": "^8.7.0"
  },
  "scripts": {
    "start": "export $(cat host.env | xargs) && node main.js",
    "docker": "export $(cat .env | xargs) && node main.js"
  }
}